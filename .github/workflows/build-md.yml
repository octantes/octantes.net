name: build-md

on:
  push:
    branches: [ main ]

permissions:
  contents: write

jobs:
  build:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v4
        with:
          persist-credentials: true

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: install deps
        run: npm ci

      - name: build SPA with Vite
        run: npm run build

      - name: copy dist to docs
        run: |
          rm -rf docs/*
          cp -r dist/* docs/
          # re-copiamos el index.json y posts (que buildstep ya generó)
          cp -r docs/index.json dist/ docs/
          cp -r docs/posts dist/ docs/

      - name: run build step
        run: node buildstep.js

      - name: commit & push generated files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/
          if git diff --cached --quiet; then
            echo "no changes to commit"
          else
            git commit -m "build: regenerar html + index.json + SPA"
            git push origin HEAD:main
          fi